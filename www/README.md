# Static files (optional)

Operators may place static assets here and pass `--www` to this directory
(or set `NANOBOT_WWW` / `WWW` in `$NANOBOT_HOME/settings`).

Nothing here is required for CLI, peer/JSON API, or MCP.
