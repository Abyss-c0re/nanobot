---
name: sync-nanobot
description: >
  Sync the nanobot project to latest Grok Build CLI and prove augogen pair-vote.
  Use when the user says "sync nanobot project", "sync nanobot", or runs /sync-nanobot.
---

Follow the user-scoped playbook (single source of truth):

`/home/voldemar/.grok/skills/sync-nanobot/SKILL.md`

Run:

```bash
bash /home/voldemar/.grok/skills/sync-nanobot/scripts/sync_nanobot.sh --sync
```
