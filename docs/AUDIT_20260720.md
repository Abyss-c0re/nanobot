# audit cycle 1 — 2026-07-20

## scope
nanobot tree only (apps, src, libs, scripts). Not rockctl/dash/commander.

## logic
| finding | sev | action |
|---------|-----|--------|
| auth seal KDF uses peer_token; rotate without re-login breaks session | med | document; deploy must never rotate token |
| peer_token auto-create only if missing | ok | keep |
| `/mnt/data/labauth` hardcoded | med | **fixed** — env / $NANOBOT_HOME/labauth only |
| dual main.c (src/ + apps/) | low | apps/ is built; src/main.c legacy dead |
| hub OUT token optional | note | use NANOBOT_OUT_TOKEN in production |
| shell denylist not sandbox | known | SECURITY.md residual |

## security
| finding | sev | action |
|---------|-----|--------|
| peer_token required for mutating peer routes | ok | fail closed |
| provider seal peer-token KDF | ok | keep |
| CORS * on peer | residual | network isolation |
| shell high privilege | residual | denylist only |
| --login clears session (intended) | ok | don't run casually on prod |
| install_remote only overwrites binary | ok | deploy_binary_safe.sh added |

## UX
| finding | sev | action |
|---------|-----|--------|
| MCP bridge tools named * | med | **fixed** → nanobot_* (+ legacy aliases) |
| docs referenced  as required | med | **fixed** BUILD.md / README |
| auth requires browser for Grok only | ok | --offline path clear |
| feature flags not visible | low | usage prints build features |

## independence
- C code: no /rockctl/Dash dependency after hardcode removal
- optional master via NANOBOT_MASTER_KEY only
- build flags for MCP/AUTH/PEER/HUB/SHELL/PROVIDERS
- bridge is product-agnostic

## deploy rule
**Never** overwrite peer_token, session, device_login, settings on robot unless human-ordered.  
Use `scripts/deploy_binary_safe.sh`.

## status
cycle 1 fixes applied; safe deploy done (see cycle 2).

---

# audit cycle 2 — post-deploy

## deploy proof (robot)
| file | result |
|------|--------|
| peer_token | **unchanged** md5 |
| settings | **unchanged** md5 |
| session | rewritten on refresh (signed_in still true) |
| peer health | OK, model grok-4.5 |

## cycle 2
| finding | action |
|---------|--------|
| session file hash changes on ensure | expected refresh |
| feature flags CLI-gate not strip objects | BUILD.md |
| slim MCP=OFF | verified |
| VERSION 0.4.0 vs cmake 0.5.0 | defer |
| src/main.c dead | defer |

## loop
await human **dismiss** → handoff; else cycle 3.
